/** Hardcoded so Easypanel build env cannot tree-shake pixel code to no-ops. */
export const PIXELS_ENABLED = true;

export const SNAP_PIXEL_ID = "";

/** TikTok Ads Manager → Assets → Events → Web Events */
export const TIKTOK_PIXEL_ID = "";
/** Old pixel kept for existing campaigns (has Event Builder patch) */
export const TIKTOK_PIXEL_ID_OLD = "";

/** Meta Events Manager */
export const META_PIXEL_ID = "";
